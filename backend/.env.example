PORT=3001
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
CORS_ORIGIN=http://localhost:8081
APP_API_KEY=

GOOGLE_CALENDAR_WEB_CLIENT_ID=
GOOGLE_CALENDAR_WEB_CLIENT_SECRET=

# --- Weather (OpenWeatherMap) ---
# Required for voice assistant weather queries (GET /api/weather).
# Get a free key: https://openweathermap.org/api
# Sign up → API keys → copy the default key (may take up to 2 hours to activate).
WEATHER_API_KEY=
