/** Hardcoded lead time — no user settings. */
export const CALENDAR_REMINDER_OFFSET_MINUTES = 30;

/** Poll Google Calendar once per minute while the app is open. */
export const CALENDAR_REMINDER_POLL_INTERVAL_MS = 60_000;
